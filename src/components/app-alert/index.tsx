"use client"

import { Toaster } from "../ui/sonner";

interface AppAlertProps {
  title?: string;
}

export function AppAlert(props: AppAlertProps) {
  return (
    <Toaster>
        
    </Toaster>
  )
}
